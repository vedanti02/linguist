import 'package:flutter/material.dart';

const kBoxdec = BoxDecoration(
    color: Colors.white, borderRadius: BorderRadius.all(Radius.circular(30.0)));

const kTextStyle = TextStyle(
    fontSize: 30.0, color: Color(0xFF6B5F6D), fontWeight: FontWeight.w800);
const Color TextColor = Color(0xFF6B5F6D);
const Color Bgcolor = Color(0xFFE9D9D9);

const online = TextStyle(fontSize: 20.0, color: Colors.black);
const offline = TextStyle(
  fontSize: 20.0,
  color: Colors.blue,
);
